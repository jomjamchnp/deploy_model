
from object_detection.db_hand import hand_detection
from object_detection.db_num import num_detection
from object_detection.image_pro import diff_img
from object_detection.score_hand import score_hand
from object_detection.score_num import score_num

def test():
    # name = '_vgh42ixnj'
    # diff_img(name)
    # num_detection(name)
    # hand_detection(name)
    # score_1,score_2,score_3 = score_num(name)
    # score_4,score_5 = score_hand(name)
    return 'ff'